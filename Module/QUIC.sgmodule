#!name=Block HTTP3/QUIC Requests.
#!desc=Block out to force a fallback to HTTP2/HTTP1.1。

[Rule]
AND,((PROTOCOL,UDP),(DEST-PORT,443)),REJECT-NO-DROP
AND,((PROTOCOL,QUIC),(DEST-PORT,443)),REJECT-NO-DROP
